<body class="recipe">

	<div class="background" style="background-image: url('{{{dir_img}}}{{{image}}}');">
	</div>

	<main>
		<section>
			<h1>{{title}}</h1>

			<div class="subtitle">
				<p>Préparation: {{preparation_time}} min</p>
				<p>Cuisson: {{cooking_time}} min</p>
			</div>

			<ul>
				{{#ingredients}}
				<li>
					{{type}}
					{{#quantity}}
						<div class="ingredients-quantity">
							{{quantity}} {{unit}}
						</div>
					{{/quantity}}
				</li>
				{{/ingredients}}
			</ul>
		</section>

		<section>
			<ol>
				{{#instructions}}
				<li>{{text}}</li>
				{{/instructions}}
			</ol>
		</section>
	</main>
</body>
