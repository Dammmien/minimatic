<!doctype html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <title>{{title}}</title>
        <link href="https://fonts.googleapis.com/css?family=Lora" rel="stylesheet" />
        <link rel="stylesheet" href="{{{project_config.dir_styles}}}main.css" />
        {{#styles}}
        <link rel="stylesheet" href="{{{project_config.dir_styles}}}{{.}}" />
        {{/styles}}
    </head>
    {{> body}}
    <script src="{{{project_config.dir_scripts}}}core.js"></script>
    {{#scripts}}
    <script src="{{{project_config.dir_scripts}}}{{.}}"></script>
    {{/scripts}}
</html>
